[phases.setup]
nixPkgs = ["python310", "gcc", "libpq"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python bot/main.py & uvicorn server:app --host 0.0.0.0 --port $PORT"
